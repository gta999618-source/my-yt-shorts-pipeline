# My YT Shorts Pipeline

Automated YouTube Shorts pipeline.

## Layout
- `src/` — Python source
- `assets/` — fonts, music, overlays
- `output/` — generated clips (gitignored)
- `state/` — processed IDs (gitignored)

## Run locally
```bash
python -m src.main
